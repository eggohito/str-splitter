#> example:translation_key/as_origin_name/display


#   Build the translation key and display it via `/tellraw`
$tellraw @s {"translate": "origin.$(namespace).$(path).name", "color": "green", "underlined": true}
